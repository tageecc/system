let os = require("os");
let util = require('../util/util');

module.exports = {
    mysql: (req, res, next) => {
        res.json({});
    },
    mongodb: (req, res, next) => {
        res.json({});
    }
};
